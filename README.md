# chedianai-carparts-sdk for Laravel

## Install
`composer require chedianai/laravel-sdk-car-parts `

## Usage
Ex. below:
+ `CarParts::carPart()->item->items()`
+ `app('chedianai.carparts')->item->items()`